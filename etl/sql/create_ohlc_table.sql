CREATE TABLE IF NOT EXISTS usd_jpy_1m (
    time TIMESTAMP PRIMARY KEY,
    open FLOAT,
    high FLOAT,
    low FLOAT,
    close FLOAT
);


CREATE TABLE IF NOT EXISTS usd_jpy_5m (
    time TIMESTAMP PRIMARY KEY,
    open FLOAT,
    high FLOAT,
    low FLOAT,
    close FLOAT
);

CREATE TABLE IF NOT EXISTS usd_jpy_30m (
    time TIMESTAMP PRIMARY KEY,
    open FLOAT,
    high FLOAT,
    low FLOAT,
    close FLOAT
);

CREATE TABLE IF NOT EXISTS usd_jpy_1h (
    time TIMESTAMP PRIMARY KEY,
    open FLOAT,
    high FLOAT,
    low FLOAT,
    close FLOAT
);

CREATE TABLE IF NOT EXISTS usd_jpy_4h (
    time TIMESTAMP PRIMARY KEY,
    open FLOAT,
    high FLOAT,
    low FLOAT,
    close FLOAT
);


